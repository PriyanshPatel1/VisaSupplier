/**
 * Sentry is currently disabled in this workspace because `@sentry/nextjs`
 * is not installed. Keep this file as a placeholder for the Next.js file
 * convention without breaking builds.
 */

export {};
